/* 
Generator() => {
    * team = []
    * getTeam(), start(), finish(), add(), build()
}

Employee() => {
    * Name, ID, Email
    * getName(), getId(), getEmail(), getRole() // Returns 'Employee'
    * validate?
    * tests
}

Manager() => {
    * officeNumber
    * getRole() // Overridden to return 'Manager'
    * validate?
}

Engineer() => {
    * github
    * getGithub(), getRole() // Overridden to return 'Engineer'
    * validate?
    * tests
}

Intern() => {
    * school
    * getSchool(), getRole() // Overridden to return 'Intern'
    * validate?
    * tests
}
*/